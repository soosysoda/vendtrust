// src/app/dashboard/page.js
// Server component — extracts params / context if needed and mounts client UI
import DashboardClient from './DashboardClient'

export default function Page() {
  return <DashboardClient />
}
